# Copy to .env (gitignored).

# VITE_API_URL              Prod build only — API base including /api, e.g. https://your-api.vercel.app/api
#                           Dev (vite): not used; apiConfig uses http://localhost:5001/api

# VITE_API_URL=https://your-api.vercel.app/api
